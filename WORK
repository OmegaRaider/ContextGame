//переделать логику

//переделать все в хедерах и cpp 

// должно быть 1.main.cpp; 2. GameClass.h; 3. MainClass.h; 4.Evil.h

// переделать enum class ( там будет оружие, в зависимости от оружия меняются характеристики)

// В BaseClass.cpp and BaseClass.h полностью все перепутано ( особенно Save )
